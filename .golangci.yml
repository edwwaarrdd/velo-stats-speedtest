version: "2"

linters:
  default: standard
  enable:
    - misspell
    - revive
  exclusions:
    presets:
      - comments
      - std-error-handling

formatters:
  enable:
    - gofmt
    - goimports
